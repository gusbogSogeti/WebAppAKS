apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-webapp.fullname" . }}
  labels:
    {{- include "aks-webapp.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-webapp.selectorLabels" . | nindent 4 }}
